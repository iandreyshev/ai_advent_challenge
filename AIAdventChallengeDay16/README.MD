# 🔥 День 16 — Локальная индексация документов с эмбеддингами

Проект выполнен в рамках курса по нейросетям (Day 16).
Цель задания — реализовать локальную индексацию документов: разбить документы на чанки, получить для них эмбеддинги и сохранить результат в виде локального индекса.

Проект не включает поиск или RAG. Он посвящён только подготовке индекса, который может быть использован на следующих этапах.

---

## 🎯 Назначение проекта

Индексация документов — базовый шаг для:

- semantic search
- Retrieval-Augmented Generation (RAG)
- интеллектуальных ассистентов по документации
- работы с большими корпусами текстов

Основная идея:
Вместо хранения «просто текста» мы сохраняем векторное представление смысла каждого фрагмента документа.

---

## 🧠 Что делает скрипт

Скрипт build_index.py реализует следующий пайплайн:

Документы → текст → чанки (фиксированный размер + overlap) → эмбеддинги (через Ollama) → локальный индекс (JSONL)

Возможности:
- рекурсивное чтение файлов из папки data/ (включая подпапки)
- поддержка документов на разных языках
- генерация эмбеддингов локально, без API-ключей
- человекочитаемые ошибки и сообщения

---

## 📂 Структура проекта

├─ data/               — документы для индексации (игнорируются git)</br>
├─ build_index.py      — основной скрипт индексации</br>
├─ index.jsonl         — локальный индекс (генерируется)</br>
├─ meta.json           — метаданные и статистика (генерируется)</br>
├─ .gitignore</br>
└─ README.md

---

## 📄 Какие файлы индексируются

По умолчанию индексируются текстовые файлы со следующими расширениями:

.txt, .md  
.py, .js, .ts, .java, .go, .rs  
.html, .css, .json, .yaml, .yml  
.sql, .sh, .zsh  

Бинарные файлы и неподдерживаемые форматы автоматически пропускаются.

---

## 🧩 Формат индекса

Индекс сохраняется в формате JSONL (index.jsonl).

Каждая строка файла — один чанк документа.

Пример записи:

{
  "id": "a1b2c3d4e5f6::0",
  "doc_id": "a1b2c3d4e5f6",
  "source": "data/docs/readme.md",
  "chunk_index": 0,
  "char_start": 0,
  "char_end": 1200,
  "text": "Текст чанка…",
  "embedding": [0.0123, -0.9812, ...],
  "model": "nomic-embed-text"
}

Файл meta.json содержит:
- параметры чанкинга
- количество обработанных файлов и чанков
- размерность эмбеддингов
- время выполнения

---

## ⚙️ Требования

- Python 3.10+
- Установленная и запущенная Ollama
- Embedding-модель: nomic-embed-text

---

## 🦙 Установка и настройка Ollama

Проверка установки:
ollama --version

Если Ollama установлена через Homebrew, запустите сервер:
ollama serve

Скачать модель эмбеддингов:
ollama pull nomic-embed-text

Проверить, что модель установлена:
ollama list

---

## ▶️ Как использовать

### 1. Подготовка данных

Положите документы в папку data/.
Подпапки поддерживаются и обрабатываются рекурсивно.

### 2. Запуск индексации (базовый)

python3 build_index.py

После выполнения будут созданы файлы:
- index.jsonl
- meta.json

### 3. Запуск с параметрами

python3 build_index.py --data-dir data --out-index index.jsonl --out-meta meta.json --model nomic-embed-text --chunk-size 1200 --overlap 200

Параметры:
- --data-dir      путь к папке с документами
- --out-index    файл индекса
- --out-meta     файл метаданных
- --model        модель эмбеддингов Ollama
- --chunk-size   размер чанка в символах
- --overlap      перекрытие чанков

---

## 📝 Примечания по реализации

- Токенизация не выполняется вручную — embedding-модель Ollama токенизирует текст внутри себя.
- Чанкинг выполнен по символам для простоты.
- Эмбеддинги генерируются полностью локально.
- Проект сфокусирован только на индексации документов.

---

## 🎬 Рекомендации для демонстрационного видео

1. Показать структуру проекта и папку data/
2. Запустить python3 build_index.py
3. Показать созданные index.jsonl и meta.json
4. Открыть одну строку index.jsonl и показать текст чанка и эмбеддинг

---

## ✅ Результат

В результате работы скрипта создаётся локальный индекс документов с эмбеддингами, готовый для дальнейшего использования в задачах semantic search и RAG.
