# 🎙️ Прямая отправка аудио в модель (Direct Mode)

## 📖 Описание

`voice_agent_direct.py` - альтернативная реализация голосового агента, которая:
1. **Записывает** аудио с микрофона в WAV файл
2. **Отправляет** аудио напрямую в модель распознавания (Whisper)
3. **Получает** распознанный текст
4. **Обрабатывает** через LLM как обычно

## 🔄 Сравнение подходов

### Оригинальная версия (voice_agent.py):
```
Микрофон → SpeechRecognition Library → Google API → Текст → LLM
```
**Плюсы:**
- Простая реализация
- Не требует дополнительных моделей
- Быстро работает

**Минусы:**
- Зависимость от библиотеки
- Ограниченный контроль
- Интернет для Google API

### Прямая версия (voice_agent_direct.py):
```
Микрофон → Запись WAV → Whisper Model → Текст → LLM
```
**Плюсы:**
- Полный контроль над процессом
- Можно использовать любую модель распознавания
- Возможность сохранять аудио для отладки
- Работает локально без интернета (с локальным Whisper)
- Более высокое качество распознавания

**Минусы:**
- Требует больше зависимостей
- Медленнее (особенно локальный Whisper)
- Больше ресурсов (RAM, GPU)

## 🚀 Установка

### Вариант 1: OpenAI Whisper API (онлайн)

```bash
# Базовые зависимости
pip install PyAudio requests

# Установите API ключ
export OPENAI_API_KEY=sk-...

# Запуск
python3 voice_agent_direct.py --mode api
```

### Вариант 2: Локальный Whisper (офлайн)

```bash
# Установка OpenAI Whisper
pip install -r requirements-direct.txt

# Или установите faster-whisper (быстрее)
pip install faster-whisper

# Запуск
python3 voice_agent_direct.py --mode local
```

### Системные зависимости

**macOS:**
```bash
brew install portaudio
brew install ffmpeg  # Опционально, для разных форматов
```

**Linux:**
```bash
sudo apt-get install portaudio19-dev python3-pyaudio
sudo apt-get install ffmpeg  # Опционально
```

## 📝 Использование

### Базовый запуск

```bash
# С OpenAI Whisper API
python3 voice_agent_direct.py --mode api

# С локальным Whisper
python3 voice_agent_direct.py --mode local
```

### Расширенные опции

```bash
# Изменить длительность записи (по умолчанию 5 сек)
python3 voice_agent_direct.py --duration 10

# Другая LLM модель
python3 voice_agent_direct.py --model llama3.2

# Изменить частоту дискретизации
python3 voice_agent_direct.py --sample-rate 44100

# Удалённый Ollama
python3 voice_agent_direct.py --host 192.168.1.100 --port 11434
```

## 🎯 Примеры сессий

### С OpenAI Whisper API:

```bash
$ export OPENAI_API_KEY=sk-...
$ python3 voice_agent_direct.py --mode api

============================================================
🎙️  ГОЛОСОВОЙ АГЕНТ (Direct Audio → Whisper → LLM)
============================================================
LLM модель: qwen2.5
Whisper режим: api
Длительность записи: 5 сек
------------------------------------------------------------
Команды: скажите 'выход' для завершения
============================================================

🎤 Говорите сейчас...
. . . . . . . . . .
✅ Запись завершена
🔄 Отправка в Whisper API...
📝 Распознано: посчитай два плюс два
🤖 Обработка запроса...

============================================================
💬 ОТВЕТ:
------------------------------------------------------------
2 + 2 = 4
============================================================
```

### С локальным Whisper:

```bash
$ python3 voice_agent_direct.py --mode local

============================================================
🎙️  ГОЛОСОВОЙ АГЕНТ (Direct Audio → Whisper → LLM)
============================================================
LLM модель: qwen2.5
Whisper режим: local
Длительность записи: 5 сек
------------------------------------------------------------

🎤 Говорите сейчас...
. . . . . . . . . .
✅ Запись завершена
🔄 Распознавание через локальную модель...
📥 Загрузка модели Whisper...
🎯 Распознавание...
📝 Распознано: расскажи анекдот
🤖 Обработка запроса...

============================================================
💬 ОТВЕТ:
------------------------------------------------------------
Программист приходит в магазин...
============================================================
```

## ⚙️ Модели Whisper

### OpenAI Whisper (локальный)

Доступные размеры моделей:

| Модель | Параметры | RAM | Скорость | Качество |
|--------|-----------|-----|----------|----------|
| tiny   | 39M       | ~1GB| Очень быстро | Низкое |
| base   | 74M       | ~1GB| Быстро | Среднее |
| small  | 244M      | ~2GB| Средне | Хорошее |
| medium | 769M      | ~5GB| Медленно | Отличное |
| large  | 1550M     | ~10GB| Очень медленно | Лучшее |

По умолчанию используется модель `base` (компромисс скорость/качество).

Изменить модель в коде:
```python
model = whisper.load_model("small")  # Вместо "base"
```

### Faster Whisper

Быстрая альтернатива OpenAI Whisper:
- В 4-5 раз быстрее
- Меньше использует памяти
- Почти такое же качество

Установка:
```bash
pip install faster-whisper
```

## 🔧 Продвинутая настройка

### Сохранение аудио файлов

Для отладки можно сохранять аудио файлы:

```python
# В voice_agent_direct.py, метод record_audio()
# Вместо tempfile.NamedTemporaryFile():
filename = f"recording_{time.time()}.wav"
```

### Использование GPU для Whisper

Для ускорения локального Whisper:

```bash
# Убедитесь, что PyTorch установлен с CUDA
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

В коде:
```python
model = whisper.load_model("base", device="cuda")
```

### Настройка качества записи

```bash
# Высокое качество (44.1 kHz)
python3 voice_agent_direct.py --sample-rate 44100

# Низкое качество, быстрее (8 kHz)
python3 voice_agent_direct.py --sample-rate 8000
```

## 📊 Производительность

### OpenAI Whisper API:
- **Скорость:** ~2-5 сек на запрос
- **Требования:** Интернет, API ключ
- **Стоимость:** $0.006 за минуту аудио
- **Качество:** Отличное

### Локальный Whisper (base):
- **Скорость:** ~5-10 сек на CPU
- **Требования:** ~1GB RAM
- **Стоимость:** Бесплатно
- **Качество:** Хорошее

### Faster Whisper (base):
- **Скорость:** ~2-3 сек на CPU
- **Требования:** ~1GB RAM
- **Стоимость:** Бесплатно
- **Качество:** Хорошее

## 🐛 Устранение неполадок

### ❌ "No module named 'whisper'"

```bash
pip install openai-whisper
```

### ❌ "CUDA out of memory"

Используйте меньшую модель или CPU:
```python
model = whisper.load_model("tiny", device="cpu")
```

### ❌ Плохое качество распознавания

1. Увеличьте длительность записи: `--duration 10`
2. Используйте большую модель: `small` или `medium`
3. Улучшите качество записи: `--sample-rate 44100`
4. Говорите чётче и громче

### ❌ "OPENAI_API_KEY not set"

```bash
export OPENAI_API_KEY=sk-...
# Или добавьте в .env файл
```

## 🔮 Дальнейшие улучшения

Возможные расширения:

1. **Автоопределение конца фразы** (Voice Activity Detection)
2. **Потоковая обработка** аудио (реал-тайм)
3. **Поддержка разных форматов** (MP3, OGG)
4. **Noise reduction** (подавление шумов)
5. **Множественные языки** автоматически
6. **Кэширование** распознанных фраз
7. **Батч-обработка** нескольких файлов

## 📚 Полезные ресурсы

- [OpenAI Whisper GitHub](https://github.com/openai/whisper)
- [Faster Whisper](https://github.com/guillaumekln/faster-whisper)
- [Whisper API Docs](https://platform.openai.com/docs/guides/speech-to-text)
- [PyAudio Documentation](https://people.csail.mit.edu/hubert/pyaudio/)

## 🆚 Когда использовать какую версию?

**Используйте `voice_agent.py` если:**
- Нужна простая реализация
- Хотите быстро запустить
- Не важен полный контроль над процессом
- Есть интернет

**Используйте `voice_agent_direct.py` если:**
- Нужен полный контроль над аудио
- Хотите работать офлайн
- Требуется высокое качество распознавания
- Планируете кастомизировать процесс
- Хотите использовать разные модели Whisper

---

**Итог:** Прямая отправка аудио даёт больше контроля и гибкости, но требует больше настроек и ресурсов.
