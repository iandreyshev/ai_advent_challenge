# День 31: Голосовой агент (Speech → LLM → Text)

Голосовой агент с распознаванием речи и текстовым выводом через LLM.

## 🎯 Две версии реализации

### 1. Базовая версия (`voice_agent.py`)
```
Микрофон → SpeechRecognition Library → Google API → Текст → LLM → Ответ
```
✅ Простая установка
✅ Быстрая работа
✅ Не требует дополнительных моделей

### 2. Прямая версия (`voice_agent_direct.py`)
```
Микрофон → Запись WAV → Whisper Model → Текст → LLM → Ответ
```
✅ Полный контроль над процессом
✅ Работает локально (офлайн)
✅ Высокое качество распознавания
📚 [Подробнее о прямой версии](DIRECT_MODE.md)

## 🔧 Установка

### 1. Установка зависимостей Python

```bash
pip install -r requirements.txt
```

### 2. Установка PyAudio (для работы с микрофоном)

**macOS:**
```bash
brew install portaudio
pip install pyaudio
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install portaudio19-dev python3-pyaudio
pip install pyaudio
```

**Windows:**
```bash
pip install pipwin
pipwin install pyaudio
```

### 3. Запуск Ollama

Убедитесь, что Ollama запущен и модель загружена:

```bash
ollama serve
ollama pull qwen2.5
```

## 🚀 Использование

### Базовый запуск

```bash
python3 voice_agent.py
```

### С параметрами

```bash
# Другая модель
python3 voice_agent.py --model llama3.2

# Удалённый Ollama
python3 voice_agent.py --host 192.168.1.100 --port 11434

# Другой язык распознавания
python3 voice_agent.py --language en-US

# Офлайн-движок (CMU Sphinx)
python3 voice_agent.py --engine sphinx
```

### Прямая версия (Direct Mode)

Для большего контроля используйте прямую отправку аудио в модель:

```bash
# С OpenAI Whisper API
export OPENAI_API_KEY=sk-...
python3 voice_agent_direct.py --mode api

# С локальным Whisper (офлайн)
pip install -r requirements-direct.txt
python3 voice_agent_direct.py --mode local

# Или через Makefile
make run-direct         # OpenAI API
make run-direct-local   # Локальный Whisper
```

📚 **Подробнее:** [DIRECT_MODE.md](DIRECT_MODE.md)

## 🎤 Движки распознавания речи

### Google Speech Recognition (по умолчанию)
- **Плюсы**: Высокое качество, поддержка русского языка
- **Минусы**: Требует интернет
- **Использование**: `--engine google`

### CMU Sphinx (офлайн)
- **Плюсы**: Работает без интернета
- **Минусы**: Среднее качество
- **Установка**: `pip install pocketsphinx`
- **Использование**: `--engine sphinx`

### OpenAI Whisper API
- **Плюсы**: Отличное качество
- **Минусы**: Требует API ключ и интернет
- **Установование**: Установите `OPENAI_API_KEY`
- **Использование**: `--engine whisper`

## 📝 Тестовые запросы

Попробуйте произнести:

1. **Математика**: "Посчитай сколько будет два плюс два"
2. **Определения**: "Дай определение искусственного интеллекта"
3. **Развлечения**: "Скажи анекдот про программистов"
4. **Факты**: "Какая самая высокая гора в мире"
5. **Советы**: "Как научиться программировать на Python"
6. **Скриншот**: "Сделай скриншот" - сохранит снимок экрана в `screenshots/screenshot_YYYYMMDD_HHMMSS.png`
7. **Запуск приложений**: "Просыпайся, папочка вернулся" - запустит все рабочие приложения

## 🎮 Команды

- **Скриншот**: Скажите "скриншот", "screenshot" или "сделай скриншот" - создаст снимок экрана и сохранит в папку `screenshots/`
- **Запуск приложений**: Скажите "просыпайся", "папочка вернулся" или "wake up" - запустит рабочие приложения:
  - Google Chrome
  - Visual Studio Code
  - Fork (Git клиент)
  - Android Studio
  - Terminal
- **Выход**: Скажите "выход", "стоп", "exit" или нажмите `Ctrl+C`
- **Повтор**: Просто говорите снова после получения ответа

**Примечание**: После выполнения любой команды агент автоматически продолжает слушать следующую команду до тех пор, пока не будет произнесена команда выхода.

## ⚙️ Настройки через переменные окружения

Создайте файл `.env`:

```bash
OLLAMA_MODEL=qwen2.5
OLLAMA_HOST=localhost
OLLAMA_PORT=11434
OPENAI_API_KEY=sk-...  # Для Whisper
```

## 🚀 Настройка списка приложений

Список приложений для команды "просыпайся" можно настроить в методе `launch_workspace_apps()` в файле `voice_agent.py` (строка ~115).

По умолчанию запускаются:
- **Google Chrome** - браузер
- **Visual Studio Code** - редактор кода
- **Fork** - Git клиент
- **Android Studio** - IDE для Android
- **Terminal** - терминал

Чтобы изменить список, отредактируйте массив `apps` для вашей операционной системы:

```python
if system == "Darwin":  # macOS
    apps = [
        ("Приложение", "Название приложения"),
        # Добавьте свои приложения здесь
    ]
```

## 🔍 Устранение неполадок

### Микрофон не работает

```bash
# Проверьте доступ к микрофону
python3 -c "import speech_recognition as sr; print(sr.Microphone.list_microphone_names())"
```

### Ollama не отвечает

```bash
# Проверьте статус Ollama
curl http://localhost:11434/api/tags

# Перезапустите Ollama
ollama serve
```

### Плохое качество распознавания

- Говорите чётко и громко
- Уменьшите фоновый шум
- Проверьте качество микрофона
- Попробуйте другой движок распознавания

### Приложения не запускаются

- Убедитесь, что приложения установлены на вашей системе
- На macOS: проверьте правильность названий приложений в папке Applications
- На Windows/Linux: проверьте пути к исполняемым файлам в коде
- Проверьте вывод программы - она покажет какие приложения успешно запущены, а какие нет
- Отредактируйте список приложений в `voice_agent.py` под свои нужды

## 📊 Сравнение версий

| Критерий | voice_agent.py | voice_agent_direct.py |
|----------|----------------|----------------------|
| **Установка** | Простая | Требует больше зависимостей |
| **Интернет** | Нужен для Google API | Опционально (локальный Whisper) |
| **Скорость** | Быстро | Медленнее (особенно local) |
| **Качество** | Хорошее | Отличное |
| **Контроль** | Ограниченный | Полный |
| **Офлайн режим** | Sphinx (среднее качество) | Whisper (отличное качество) |
| **Использование** | Для быстрого старта | Для продакшена |

**Рекомендация:** Начните с `voice_agent.py`, затем переходите на `voice_agent_direct.py` для лучшего качества.

## 📊 Технологии

- **SpeechRecognition**: Распознавание речи
- **PyAudio**: Работа с микрофоном
- **Pillow**: Создание скриншотов экрана
- **Ollama**: Локальный LLM
- **Python 3.8+**: Основной язык

## 🎯 Особенности

- ✅ Поддержка нескольких движков распознавания
- ✅ Автоматическая калибровка микрофона
- ✅ Гибкая настройка через CLI аргументы
- ✅ Поддержка локальных и удалённых LLM
- ✅ Обработка ошибок и таймаутов
- ✅ Интерактивный режим с циклом запросов
- ✅ Создание скриншотов экрана по голосовой команде
- ✅ Автоматический запуск рабочих приложений (Chrome, VS Code, Fork, Android Studio, Terminal)
- ✅ Кроссплатформенность (macOS, Windows, Linux)
