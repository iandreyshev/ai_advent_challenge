# День 32: God Agent ⚡️

Универсальный персональный AI-ассистент, объединяющий все лучшие возможности из предыдущих проектов AI Advent Challenge.

## 🎯 Возможности

### 🧠 Персональный интеллект
- **Профиль пользователя** — загружает ваши предпочтения, привычки, контекст из YAML
- **Долгосрочная память** — запоминает факты между сессиями (автоматически и вручную)
- **Адаптивное поведение** — подстраивается под ваш стиль общения

### 🎤 Голосовое управление
- **Распознавание речи** — Google (онлайн), Sphinx (офлайн), Whisper (API)
- **Голосовые команды** — скриншоты, запуск приложений, загрузка данных
- **Текстовый вывод** — отвечает текстом через LLM

### 📊 Анализ данных
- **CSV/JSON** — загрузка и анализ табличных данных
- **Статистика** — автоматический частотный анализ
- **Умные запросы** — задавайте вопросы по данным через LLM

### 🛠 Инструменты
- **Скриншоты** — захват экрана по команде
- **Запуск приложений** — открывает ваш рабочий набор (Chrome, VS Code, Android Studio, etc.)
- **Смена моделей** — переключение между Ollama моделями на лету

## 📦 Установка

### 1. Зависимости Python

```bash
make install
# или
pip3 install -r requirements.txt
```

**Для macOS (PyAudio):**
```bash
brew install portaudio
pip3 install pyaudio
```

### 2. Ollama

```bash
# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Установка модели (рекомендуемая)
ollama pull llama3.1
```

### 3. Запуск Ollama сервера

```bash
ollama serve
```

## 🚀 Использование

### Текстовый режим

```bash
python3 god_agent.py
# или
make run
```

### Голосовой режим

```bash
python3 god_agent.py --voice
# или
make voice
```

### С кастомным профилем

```bash
python3 god_agent.py --profile my_profile.yaml
```

### С другой моделью

```bash
# По умолчанию используется llama3.1
python3 god_agent.py --model qwen2.5
python3 god_agent.py --model llama3.2  # быстрее, но менее точная
```

### Голосовой режим с офлайн распознаванием

```bash
python3 god_agent.py --voice --engine sphinx
```

## 📋 Команды

### Профиль и память
- `/profile` — показать профиль пользователя
- `/remember <факт>` — запомнить факт вручную
- `/memory` — показать все запомненные факты
- `/forget` — очистить память

### Управление
- `/clear` — очистить историю чата
- `/model <имя>` — сменить модель Ollama
- `/help` — справка по командам

### Инструменты
- `/load <файл>` — загрузить данные (CSV/JSON) для анализа
- `/screenshot` — сделать скриншот экрана
- `/app <название>` — запустить приложение (умный поиск по установленным)
- `/apps [фильтр]` — показать список установленных приложений
- `/launch` — запустить рабочее окружение (Chrome, VS Code, Terminal)

### Голосовые команды (в режиме --voice)

**ИИ понимает намерение** — говорите своими словами, не нужно запоминать точные фразы!

**Запуск приложений (умный поиск):**
- **"открой телеграм"** / **"запусти telegram"** → `/app telegram`
- **"включи музыку"** / **"открой spotify"** → `/app spotify`
- **"открой браузер"** / **"запусти хром"** → `/app chrome`
- **"открой редактор кода"** / **"запусти vs code"** → `/app visual studio code`
- **"покажи почту"** / **"открой mail"** → `/app mail`

Агент сам найдёт подходящее приложение среди установленных!

**Другие команды:**
- **"сделай скриншот"** / **"сфоткай экран"** → `/screenshot`
- **"просыпайся"** / **"рабочие программы"** → `/launch` (Chrome + VS Code + Terminal)
- **"покажи профиль"** / **"кто я?"** → `/profile`
- **"что ты помнишь?"** / **"память"** → `/memory`
- **"запомни что я люблю кофе"** → `/remember ...`
- **"загрузи файл data.csv"** → `/load ...`
- **"очисти историю"** → `/clear`
- **"справка"** / **"помощь"** → `/help`
- **"пока"** / **"выход"** → завершение работы

Всё остальное обрабатывается как обычный вопрос к ИИ.

### Выход
- `exit`, `quit`, `q`, `/exit`, `/quit`

## 🗂 Структура проекта

```
AIAdventChallengeDay32/
├── god_agent.py           # Главный файл
├── tools/                 # Модули
│   ├── llm.py            # Работа с Ollama
│   ├── memory.py         # Система памяти
│   ├── profile.py        # Профиль пользователя
│   ├── analytics.py      # Анализ данных
│   ├── voice.py          # Распознавание речи
│   ├── screenshot.py     # Скриншоты
│   └── launcher.py       # Запуск приложений
├── profile.yaml          # Ваш профиль
├── memory.json           # Память (создаётся автоматически)
├── screenshots/          # Папка со скриншотами
├── requirements.txt      # Зависимости
├── Makefile             # Команды запуска
└── README.md            # Документация
```

## ⚙️ Настройка профиля

Отредактируйте `profile.yaml`:

```yaml
name: "Ваше имя"
role: "Ваша роль"

preferences:
  language: "русский"
  style: "дружелюбный"
  detail_level: "подробно"
  topics:
    - "программирование"
    - "AI"

habits:
  work_hours: "9:00-18:00"
  tools:
    - "VS Code"
    - "Git"

context:
  current_projects:
    - "Мой проект"
  goals:
    - "Изучить LLM"
  tech_stack:
    - "Python"
    - "Swift"

agent:
  name: "Мой Ассистент"
  tone: "дружелюбный"
  behavior: "отвечай кратко"
  greeting: "Привет, {user_name}!"
```

## 📝 Примеры использования

### Анализ данных

```
Вы: /load sample_data/sales.csv
God Agent: ✅ Загружено 150 строк, 5 колонок
           [показывает сводку]

Вы: Какой средний чек?
God Agent: Средний чек составляет 1250 рублей...

Вы: Покажи топ-5 товаров по продажам
God Agent: [анализ данных]
```

### Запоминание фактов

```
Вы: Я предпочитаю Swift вместо Objective-C
God Agent: [ответ]
          💾 [запомнил: Иван предпочитает Swift вместо Objective-C]

Вы: /remember Я использую dark mode везде
God Agent: ✅ Запомнил: Иван использует dark mode везде

Вы: /memory
God Agent: [показывает все факты]
```

### Голосовое управление

```bash
python3 god_agent.py --voice

🎤 Говорите...
Вы: "Скриншот"
God Agent: ✅ Скриншот сохранён: screenshots/screenshot_20260129_143522.png

Вы: "Просыпайся"
God Agent: 🚀 Запуск приложений...
          ✅ Запущено: Google Chrome, VS Code, Fork...

Вы: "Сколько будет два плюс два?"
God Agent: Два плюс два равно четыре.
```

## 🎥 Демонстрация

Создайте видео с демонстрацией:

1. Запустите в голосовом режиме: `make voice`
2. Покажите:
   - Голосовое взаимодействие
   - Создание скриншота
   - Запуск приложений
   - Загрузку и анализ данных
   - Работу с памятью
   - Смену модели

## 🔧 Troubleshooting

### Ollama не подключается
```bash
# Проверьте, что сервер запущен
ollama serve

# Проверьте модель
ollama list
ollama pull qwen2.5
```

### PyAudio не устанавливается (macOS)
```bash
brew install portaudio
pip3 install pyaudio
```

### Голосовое распознавание не работает
```bash
# Проверьте микрофон
python3 -c "import speech_recognition as sr; print(sr.Microphone.list_microphone_names())"

# Используйте офлайн режим
python3 god_agent.py --voice --engine sphinx
pip3 install pocketsphinx
```

## 🌟 Особенности

- **Модульная архитектура** — каждый инструмент независим
- **Расширяемость** — легко добавить новые возможности
- **Персонализация** — адаптируется под вашу работу
- **Локальность** — работает без интернета (с Ollama + Sphinx)
- **Кроссплатформенность** — macOS, Windows, Linux

## 📚 Источники

God Agent объединяет наработки из:
- **День 31**: Голосовой агент (speech recognition, screenshots, launcher)
- **День 30**: Персональный агент (memory, profile)
- **День 29**: Аналитик данных (CSV/JSON analysis)
- **Дни 12-23**: MCP серверы (архитектурные паттерны)
- **Дни 16-20**: RAG системы (работа с документами)

## 🎓 Итоги AI Advent Challenge

Это финальный проект курса, демонстрирующий:
✅ Интеграцию множества AI-паттернов
✅ Модульную архитектуру
✅ Практическое применение LLM
✅ Персонализацию и память
✅ Мультимодальность (голос + текст)
✅ Работу с данными

---

**God Agent** — ваш универсальный AI-помощник! ⚡️
