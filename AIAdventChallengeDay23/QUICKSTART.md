# Быстрый старт

## Подготовка (5 минут)

### 1. Установить Ollama
```bash
# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Windows - скачать с https://ollama.com/download
```

### 2. Загрузить модели
```bash
# Модель для RAG (векторный поиск)
ollama pull nomic-embed-text

# Модель для генерации с tool calling (ВАЖНО!)
ollama pull llama3.2
```

### 3. Запустить Ollama
```bash
ollama serve
```

## Запуск (2 минуты)

### Терминал 1: MCP-сервер
```bash
cd tasks-mcp
./gradlew run
```

Дождитесь сообщения: `Application started on http://localhost:8080`

### Терминал 2: Ассистент команды
```bash
cd team-assistant
./gradlew run
```

Должно появиться:
```
🚀 TaskFlow Team Assistant ready with:
   • RAG: N chunks indexed
   • MCP: 5 tools connected natively to LLM
   • LLM: llama3.2 via Ollama (native tool calling)
```

## Что произойдет

1. Загрузка и индексация документации (~30 сек)
2. Автоматический запуск 5 тестовых запросов
3. llama3.2 **сама** будет вызывать нужные MCP инструменты
4. Вывод ответов с рекомендациями в консоль

## Тестовые вопросы

1. "Покажи задачи с приоритетом high и предложи, что делать первым"
2. "Какие задачи у Ивана Петрова? Что он сейчас делает?"
3. "Расскажи про задачу task001 - что там с багом OAuth?"
4. "Какой статус проекта TaskFlow Core? Есть ли проблемные задачи?"
5. "Какие задачи заблокированы и почему?"

## Ожидаемый результат

Для каждого вопроса:
- ✅ Найдена релевантная документация (RAG)
- ✅ llama3.2 сама вызвала нужные MCP инструменты
- ✅ В логах видно: `🔧 Executing MCP tool: search_tasks with args: ...`
- ✅ Сгенерирован подробный ответ с рекомендациями

## Как работает tool calling

llama3.2 **самостоятельно** решает какие инструменты вызывать:

```
Вопрос → llama3.2 анализирует → Вызывает MCP tools → Анализирует результаты → Ответ
```

**Пример:**
```
Вопрос: "Покажи задачи с high priority"

llama3.2:
  1. Вызывает: search_tasks(priority="high")
  2. Получает: список из 4 задач
  3. Может дополнительно вызвать: search_tasks(status="blocked")
  4. Анализирует все данные
  5. Генерирует ответ с приоритизацией
```

## Troubleshooting

### Ollama не отвечает
```bash
curl http://localhost:11434/api/tags
```

Должны быть модели: `nomic-embed-text` и `llama3.2`

### llama3.2 не найдена
```bash
ollama list  # Проверить установленные модели
ollama pull llama3.2  # Загрузить
```

### Порт 8080 занят
Измените в `tasks-mcp/src/main/resources/application.yaml`:
```yaml
ktor:
  deployment:
    port: 8081  # другой порт
```

### Tool calling не работает

Убедитесь:
1. Используется именно `llama3.2` (не llama3.1)
2. MCP-сервер работает на localhost:8080
3. В логах есть строки `🔧 Executing MCP tool:`

## Структура данных

- **Пользователи:** 4 члена команды (developer, frontend, qa, lead)
- **Проекты:** TaskFlow Core, TaskFlow Mobile
- **Задачи:** 10 задач с разными приоритетами и статусами
- **Документация:** team_management.txt, tasks.txt, и другие

## Дальше

Смотрите полную документацию в [README.md](README.md) и [SOLUTION_DAY23.md](SOLUTION_DAY23.md)
